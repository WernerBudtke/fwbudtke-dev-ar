# fwbudtke-dev-ar
Personal site - WIP
Scaffold: Vite + React + TypeScript landing page.

Quick start (using `yarn`):

```bash
yarn
yarn dev
# build for production
yarn build
# deploy to GitHub Pages (requires `yarn deploy` to be run from CI or locally after build)
yarn deploy
```